version: '3.8'

services:
  frontend:
    build: ./frontend
    container_name: facubus-frontend
    ports:
      - "5173:80"  
    networks:
      - facubus-network

networks:
  facubus-network:
    driver: bridge
